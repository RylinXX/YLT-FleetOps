# 验证记录

日期：2026-09-05。

- `npm test`：10 项通过，覆盖分页映射、日期序列化、固定筛选、参数拒绝、无凭证转发、业务失败保留、跨站与写操作拒绝、非 JSON、超时、限流和响应大小限制。
- `npm run verify:live`：6 项查询各取第 1 页 1 条，以及 1 次区划字典；全部 HTTP 200 / code 2000 / success true。详细结果见 `verification.json`，无真实行内容落盘。
- `npm run build`：通过；主 JavaScript 35.82 kB，gzip 13.36 kB（本次构建）。
- 浏览器实际操作：六项查询分别成功返回，字段映射正常；企业查询从第 1 页翻到第 2 页，序号随分页变化；虚构企业名查询得到空结果；海淀区筛选返回 27 条总数，取出的 5 条区域一致；更换筛选自动回第 1 页；JSON、cURL、JavaScript 和调用历史可切换。
- 浏览器发现并修复：短窗口侧栏第 6 个入口被挤出可视区、复合名称图标未正确渲染、变更筛选未重置页码。
- 1440 x 1000、390 x 844、360 x 800 布局测量：页面无横向溢出，表单字段没有超出容器；手机可切换第 6 项接口；小屏目录含全部六项。
- 来源图片完成加载（1496 x 690），所有图标完成替换；原站与本地结果未经模型改写。
- 三类导出文件均可读取，分别含 7 个只读接口。OpenAPI 来自人工核验结构及生成脚本，未在第三方 OpenAPI 客户端逐项测试导入。

截图：`screenshots/desktop.png`、`screenshots/mobile.png`。截图包含当次公开查询的部分显示内容，仅保存在本地，不会被前端公开提供。

仍未验证：所有筛选组合及日期边界、上游长期可用性、完整 CORS 策略、迁移后新系统的接口、公开互联网部署及其授权。
